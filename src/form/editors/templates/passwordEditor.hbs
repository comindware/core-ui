{{#if maxLength}}
    <input type="Password" class="input js-input" placeholder="{{placeholder}}" maxlength="{{maxLength}}">
{{else}}
    <input type="Password" class="input js-input" placeholder="{{placeholder}}">
{{/if}}