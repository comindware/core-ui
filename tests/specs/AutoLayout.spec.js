
import { initializeCore } from '../utils/helpers';
import 'jasmine-jquery';

describe('Layout', () => {
    beforeEach(() => {
        this.rootRegion = initializeCore();
    });

    describe('AutoLayout', () => {
        //Todo test
    });
});
